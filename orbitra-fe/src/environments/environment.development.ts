// Config used for local development (ng serve) - angular.json's fileReplacements
// swaps this in automatically for the "development" build configuration.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080', // the API Gateway, the single entry point for every backend service
};
