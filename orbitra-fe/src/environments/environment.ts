// Config used for production builds (ng build) - this is the default file;
// only swapped out for environment.development.ts during local dev.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8080', // placeholder until this app is actually deployed somewhere real
};
